# Ready to delete — retired reference-file versions

Integer versioning: the largest integer is the newest. Superseded versions are
listed here rather than edited in place, so a pipeline run always pins an exact
version and an old result stays reproducible against the file it actually used.

**Do not delete anything on this list until no committed manifest references
it.** A manifest records the SHA-256 of the reference files a run consumed;
deleting a file that a manifest names makes that run unverifiable.

| File | Superseded by | Date retired | Why | Safe to delete |
|---|---|---|---|---|
| `reference/pin1_covalent_cys113_anchors_1.csv` | `_2` | 2026-07-27 | The Reddi 2023 sulfamate-acetamide anchor was `UNVERIFIED`; Figure 5 of the paper resolved it into two verified anchors (4d, 4g). Anchor count 6 → 7, verified 4 → 6. | not yet — no runs consumed it, but keep until M0 is git-tagged |
| `reference/warhead_classes_1.csv` | `_2` | 2026-07-27 | `sulfamate_acetamide` moved `UNVERIFIED` → `VERIFIED`; `sulfonate_acetamide` (compound 4a) added. Enumerable classes 1 → 3. | not yet — same reason |

| `reference/pin1_reference_binders_1.csv` | `_2` | 2026-07-27 | Two rows were attributed to ChEMBL **CHEMBL3391**, which is *Threonine—tRNA ligase 1*, **not Pin1** (Pin1 is CHEMBL2288) — verified directly against the ChEMBL API. Both removed. Eight new actives added from CHEMBL2288 + literature; the resolved Reddi 4d/4g structures folded in. 11 rows → 18. | not yet — no runs consumed it |

| `decoys/decoys_covalent_1.csv` | `_2` | 2026-07-27 | Only 32 of 302 decoys (10.6%) carried any electrophile, so ~90% could not be covalently docked at all — gnina needs a reactive atom to bond to. The surviving comparison would have been "electrophiles vs inert molecules", which docking wins trivially. `_2` requires every decoy to carry a warhead motif. | not yet — no runs consumed it |

| `append_only/.../D4_1.parquet` | `D4_2` | 2026-07-27 | Enumerated with a broken `snar_chloroazine` fragment that omitted the chlorine, so the warhead-validity gate correctly killed all 198 of that class. `D4_2` has the corrected fragment. | not yet |

| `reference/pin1_reference_binders_2.csv` | `_3` | 2026-07-30 | Two actives added from a systematic ChEMBL2288 + literature sweep. **Liu-2024-C3** (non-covalent, Kd 130 nM, co-crystal 9INR) takes the non-covalent gate from 5 to **6 independent chemotypes** — the gate's floor, so T_1/T_2 can reach a verdict other than UNDERPOWERED for the first time. **Ieda-2019-(S)-2** (cinnamamide, MS-confirmed Cys113 adduct) takes the covalent side from 4 to 5. 18 rows → 20. | not yet — the gate has not been re-run against `_3` |

| `reference/warhead_classes_8.csv`, `decoy_chemotypes_2.csv` | `_9`, `_3` | 2026-07-30 | `_9` adds `snar_chloroazine_general`, a decoy-side class test whose attachment pattern describes SNAr chemistry rather than Tian 6a specifically; `_3` relaxes the matching decoy chemotype. The class pool goes 3 → 1449 adduct-valid and Tian becomes testable. See D0042. **`_8` was never loaded by anything** — `warhead_library.DEFAULT_LIBRARY` still pointed at `_7`, so the acrylamide precedent correction in `_8` had no effect until this change. | not yet |

| `immutable/.../class_pools_3/` | `append_only/.../class_pools_4/` | 2026-07-30 | Pools retrieved with the OLD narrow chemotype queries. The cache is keyed on `class_id` alone, so after the queries were relaxed it silently served the stale 3-molecule snar pool. A new directory rather than a refresh, so an old query's results cannot be served under a new query's name. | not yet — `class_pools_3` lives in the read-only tree and is referenced by earlier manifests |

| `reference/warhead_classes_7.csv` | `_8` | 2026-07-30 | The acrylamide row asserted "NO validated covalent-Cys113 Pin1 precedent". That is now false for the class: Ieda 2019 (PMID 30585173) reports an aryl-substituted member with an ESI-MS-confirmed Cys113 adduct. The parent acrylamide still has none, and `_8` says so precisely rather than either over- or under-claiming. | not yet |

| `append_only/.../D1_15.parquet`, `D2_15`, `D3_14`, `D4_27` | `D1_16`, `D2_16`, `D3_15`, `D4_28` | 2026-07-30 | Written from an uncommitted working tree, so each manifest stamps commit `070f1aa` while the code that produced it was `9785e48`. The numbers are byte-identical — the merge was re-run unchanged against a clean tree solely so the provenance stamp is true. Keeping both would preserve a version whose only distinguishing feature is a false attribution. | yes, once nothing references them — no downstream run consumed them |

| `append_only/.../01_t1_de_novo/gromacs/t1_1224c0ee20c2/rep1/step*.pdb` | — | 2026-07-30 | ~1400 LINCS constraint-violation dumps from a production run that exploded. They are GROMACS diagnostics for a failure already recorded in `rep1_traceback.txt`, and carry no information the traceback lacks. Any candidate that blows up will produce a similar pile. | yes — but the protected-path hook blocks deletion under `/data/lab_vm`, so this needs a route around it |

| `append_only/.../99_smoke_degree2/` | — | 2026-07-31 | Smoke test of `scripts/sample_t2_degree2.py`: 12 of ATRA's 1,882 degree-1 parents, 307 molecules kept at p=0.00603. Written only to verify the sampler's global dedup, the governor and the Bernoulli draw before committing a real run. `99_` prefixed so it sorts away from the real experiments and cannot be mistaken for one. It confirmed the degree-2 population estimate to 1.01x and has no other value. | yes — nothing consumes it |

| `append_only/.../02*_t2_*/docking/ligands/` + `poses/` | `ligands_ph7.4/`, `poses_ph7.4/` | 2026-07-31 | Ligands prepared as the NEUTRAL drawn form — `Chem.AddHs()` on neutral SMILES and a bare `obabel` call with no `-p`. Wrong for ~85% of four of the five T_2 seed pools (phosphate, carboxylic acid or basic amine). Superseded by pH-7.4 preparation; verified to change H-bond donor typing (HD counts 3→2, 2→1 on acids, 0→1 on amines). The new directory is tagged rather than refreshed **because the prep cache short-circuits on `if out.is_file()` and was keyed on candidate_id alone** — refreshing in place would have silently re-docked the stale neutral files. Raised by @tt8804 in issue #5. | not yet — the neutral frames are still the latest D2 for atra, and nothing has replaced them |

| `reference/pin1_reference_binders_3.csv` | `_4` | 2026-07-31 | `_4` adds two potent covalent Cys113 actives the gate had been calling UNDERPOWERED without — **ZL-Pin13** (IC50 67 nM, PDB 7F0M) and **164A10** (IC50 4–20 nM, PDB 8VJG), both PDB entries verified against RCSB before adding. It also splits free-text `potency` into `potency_value_nM` / `potency_unit` / `potency_type` (the original string is preserved, so a mis-parse cannot destroy the source), and adds a `tier` column moving the five promiscuity-flagged entries (juglone, EGCG, ATRA, KPT-6566, PiB) out of `lead` — they carry no usable number and plausibly depress the gate AUC. **ATRA remains T_2's seed**; demoting a gate active is not removing a starting point. | not yet — `_3` is named in D0041's `affects:` list and in earlier manifests |

| `append_only/.../01_t1_de_novo/D1_2{1,2,3}`, `02_t2_atra_crem/D2_2{1,2,3}` | `D1_24` / `D2_24` | 2026-08-01 | Carry `explicit_rmsd_replicate_sd_x`/`_y` (and `_min`, `_max`, `_ratio`, `n_replicates`, `explicit_rmsd_suspect_any`) with **nothing under the canonical name**, because `merge_gromacs_results.py` dropped a hand-maintained column list that omitted every aggregate the same function builds a few lines above it. Anything reading the plain name got nothing. Fixed by deriving the drop list from the merge; `_24` onward verified to carry 0 suffixed columns. | yes — superseded and defective |

| `append_only/.../01_t1_de_novo/D1_25`, `02_t2_atra_crem/D2_27`, `02d_t2_duxu_crem/D2_5`, `02e_t2_guo_crem/D2_5`, `03_t3_reinvent/D3_21`, `04_t4_combinatorial/D4_33` | superseded twice — current is `D1_27` / `D2_29` / `D2_7` / `D2_7` / `D3_23` / `D4_35` | 2026-08-01 | The first size-decorrelated re-rank (D0049), written before D0051 landed. The T_1 and T_2 members are **wrong in a way that matters**: they carry `rank_validated = True` against a `WEAK` gate verdict, because `attach_gate` tested validity as a denylist and `WEAK` was not on it. The T_3/T_4 members are merely superseded — the covalent gate was `UNDERPOWERED`, which the old denylist did cover, so their flag was already correct. | yes — superseded within the hour, and the non-covalent pair assert a validity the project denies |

| `append_only/.../01_t1_de_novo/D1_26`, `02_t2_atra_crem/D2_28`, `02d_t2_duxu_crem/D2_6`, `02e_t2_guo_crem/D2_6`, `03_t3_reinvent/D3_22`, `04_t4_combinatorial/D4_34` | `D1_27` / `D2_29` / `D2_7` / `D2_7` / `D3_23` / `D4_35` | 2026-08-01 | Correct in content — D0049 decorrelation and D0051's allowlist both applied — but `rank`, `rank_raw_metric` and `group_n_docked` are `float64`, because they are built by assigning into columns seeded with `pd.NA`. A rank therefore reads as `304.0` in the GUI and in any CSV export. Re-ranked with those cast to nullable `Int64`. **No number changed**, only the dtype. | yes — superseded, and no result depends on them |

| `append_only/.../02_t2_atra_crem/D2_2{5,6}`, `01_t1_de_novo/D1_24`, `03_t3_reinvent/D3_20`, `04_t4_combinatorial/D4_32` | the re-ranked frames above | 2026-08-01 | Pre-decorrelation rankings: ranked on the raw metric, which D0043 measured as partly a molecular-size sort (T_1 rho -0.617, T_3 -0.695). Superseded by D0049. `D2_26` additionally carries the `rank_validated = True` defect of D0051. Otherwise **not defective** — they were correct under the decision in force when written, and `rank_raw_metric` on the new frames reproduces their ordering. | not yet — keep one generation back until the re-ranked frames have been read in the GUI |
| `append_only/.../99_smoke_reservoir/` | — | 2026-08-05 | Smoke test of the reservoir rewrite of `sample_t2_degree2.py`: 20 ATRA parents, target 500, kept exactly 500 of a MEASURED population of 96,771. Written only to verify the draw returns the exact target and records the sampling fraction as a result rather than an assumption, before committing ~1,800 CPU-hours across five seeds. `99_` prefixed so it sorts away from the real experiments and cannot be mistaken for one. | yes — nothing consumes it |

| `append_only/.../00_outputs/blacksmith/coord_modes/{modes,molecules,summary}_nac_v5_1.*` | `..._nac_v5_2.*` | 2026-08-19 | Smoke run of `exp/7_coord_modes` on **6 molecules** (`--limit 6`), written before the full 561-molecule pass. Same filename stem as the real result and differs only by the version integer, so a reader who globs the stem instead of taking the newest gets 2,542 poses where the run clustered 236,299. Nothing consumes it. | yes — a 1% sample wearing the run's name |

| `append_only/.../00_outputs/artist/manuscript_figures/zz_calib_anchors_1.{pdf,png}` | — | 2026-08-25 | Calibration render of the manuscript GUI figure, with blue rings drawn at each callout's anchor point. Written to settle where the arrows actually landed after they resolved at roughly half their intended depth: `annotate()` with a data-coordinate `xytext` outside the image autoscales the axes, so each callout measured a different panel height from the one before it. The arrows still looked plausible, which is why the ruled overlay was needed. Fixed by freezing the limits in `manuscript_gui_figure.py`. | yes — a diagnostic, and the defect it diagnosed is fixed |

| `append_only/.../00_outputs/artist/manuscript_figures/{f2_gui_output,f3_control_walked_to_chance,f4_written_substrate,f5_levels_of_theory}_*` below the highest integer | the highest integer of each stem | 2026-08-25 | Intermediate renders of the four manuscript figures, superseded during layout work (label collisions, an unlabelled reference band, a clipped annotation, a title overclaiming "five levels" where the panel showed two). Content-identical numbers throughout — every version parses its counts from the repo at render time — so only the layout differs. | yes — keep the highest integer of each stem |

| `append_only/.../00_outputs/artist/manuscript_figures/{pin1_combined_figure,pin1_fig1_interface,pin1_fig2_controls}_*` below the highest integer | the highest integer of each stem | 2026-08-25 | Intermediate renders. `pin1_combined_figure_*` is the single-page version, superseded once the two themes were split. Within the surviving stems: `_1`/`_2` predate a caption-layout fix (runs were measured at `figure.dpi` 200 and rasterised at `savefig.dpi` 600, so every bold panel letter was drawn on top of the preceding full stop); `_3`/`_5`/`_6` predate Fig 1c becoming an interaction map on the same candidate as panel b; `_6` predates a legend entry that advanced by a length threshold guessed against a different wrap width, putting "proline-binding pocket" on top of the next entry; and `_7` draws the 18 contacting Pin1 residues as sticks, which buries the pocket shape the panel exists to show (@mhallet, 2026-08-25). All counts are identical throughout -- every version parses them from the repo at render time. | yes -- keep the highest integer of each stem |

## Note on the governed data root

Large derived outputs live under `/data/lab_vm/append_only/inhibition/` and are
never overwritten. When an approach is re-run and supersedes an earlier
integer-versioned output there, record the retired version here too — the
append-only tree grows, so this file is how it gets pruned safely.

Nothing under `/data/lab_vm/immutable/inhibition/` is ever listed here. That
tree is read-only source (6VAJ, model weights, fragment DBs, decoys) and is not
retired by this process.

| `shared/pose_cluster.py` | `shared/pose_contacts.py` | 2026-08-27 | HDBSCAN over heavy-atom RMSD, proposed in D0090 and never adopted. **No production caller** — only `exp/4,5,6,7,8,9,10` and their tests, which are the record of why: it discards 29% of a cloud as noise, lost the MD-validated pose in 3 of 30 replicates, kept 1 of 3 modes across an independent draw (#78), and its cluster count grows linearly with sampling because HDBSCAN has no length scale. Superseded by contact-space complete linkage, which bounds within-group distance structurally (D0092, D0095). | not yet — those experiments still import it; delete when they are archived |

| `append_only/.../nac_split_smoke/`, `nac_split_smoke_dbscan/` | — | 2026-08-27 | Two one-molecule, 200-run smoke topics written to prove `splitting.method` dispatches to both splitters end to end (D0097). Not a screen; nothing downstream reads them. | yes — no manifest references them |
